1. I can see 2 folders named test in every user regardless of creating them or sharing them, check them and remove those if they are anywhere close to hardcoded
2. when trying to download a file once uploaded to the drive, it shows not authenticated, find root cause of the issue by auditing whole codebase(only files and sections relevent to the file sharing and download)
3. make sure the codebase supports folder creation as per clouddrive
4. Only edit files and code but dont run automated tests
